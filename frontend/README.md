Frontend (squelette)

Commandes:
- npm ci
- npm run dev
- npm run build

Le frontend attend l'API backend sur le même hôte (proxy via docker-compose). Le chart est un placeholder; intégrer ApexCharts ou TradingView pour la suite.
