# TEST_CHECKLIST

- [ ] backend: npm run test (unit) succeeds (or placeholder test present)
- [ ] frontend: npm run build succeeds
- [ ] docker-compose up --build completes and services healthy
- [ ] DB schema created (prices table exists)
- [ ] seed_symbols script runs without error
- [ ] smoke curl to /markets/latest returns 200
