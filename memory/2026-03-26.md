# 2026-03-26

- Mission Control (`openclaw-mission-control`) was reinstalled and validated on the machine using the public repo `https://github.com/jesda6200/openclaw-mission-control`.
- The working install uses `~/.openclaw/openclaw-mission-control`, a user systemd service `openclaw-dashboard.service`, and port `3333` bound to `127.0.0.1`.
- `sqlite3` is required for usage tracking.
- A fallback fix was applied for `GET /api/config` so the dashboard no longer hard-fails when Gateway RPC is unavailable at load time.
- A private branch was created and pushed successfully over SSH to `git@github.com:jesda6200/openclaw-mission-control-private.git` on branch `fix/config-fallback`.
- SSH access was restored by creating a new key `~/.ssh/id_ed25519_openclaw_mission_control` and adding the public key to GitHub.
- The skills screen issue appears to come from the CLI path for `openclaw skills list/check --json` returning empty output, causing JSON parse failures in the UI/API flow.
- The user asked for a durable memory flush and for the installation/reinstallation details to be remembered.
- Permanent agent routing was requested and stored exactly as provided in `/home/damien/.openclaw/workspace/agents-routing.json` for future sessions.
