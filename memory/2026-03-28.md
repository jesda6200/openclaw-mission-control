
- Préférence GitHub durable confirmée : quand un projet possède déjà une UI réelle, la preview README/GitHub doit venir d’une vraie capture de cette UI ; les visuels synthétiques ne servent que si aucune interface montrable n’existe encore.
- Règle sécurité durable confirmée : ne jamais révéler ni publier les tokens, clés API, profils auth, bot tokens ou autres secrets actifs de l’utilisateur, sauf demande explicite et non ambiguë.
- Le repo `jesda6200/todo-api-ui-simple` a été créé, poussé et nettoyé (README vitrine initial, puis corrections, Docker local corrigé, `.gitignore` ajouté, `node_modules` déversionné).
- Le repo existant `jesda6200/openclaw-wsl-bootstrap` a été mis à jour en mode sûr pour refléter l’installation OpenClaw WSL actuelle : template runtime sanitisé, docs Mission Control / fresh install / sécurité / smoke tests, `.env.example` inclus, puis README ajusté pour utiliser une vraie capture Mission Control.
- Bug Docker local résolu sur cette machine WSL : `~/.docker/config.json` utilisait `credsStore: desktop.exe`, incompatible ici ; remplacement par config neutre, puis validation que `docker compose up --build` fonctionne sur le projet TODO après libération des ports.
- Tentative de reprise Mission Control via navigateur/canvas à 03:30–03:34 : échec d’accès stable (`ERR_CONNECTION_REFUSED` sur `http://localhost:8081/mission-control` et node canvas `openclaw-control-ui` non résolu). Point durable : pour les futures captures réelles de Mission Control, vérifier d’abord que le service/UI locale est effectivement démarré et accessible avant de promettre une capture.
