- L’utilisateur veut que Mission Control reflète une vraie délégation métier en arborescence : MAIN -> INTELLIGENCE -> ARCHI -> PRODUCTION -> OPS -> CONTROL -> EXPERTISE.
- Le routage actif d’OpenClaw confirmé par `openclaw agents` vient de `bindings` dans `~/.openclaw/openclaw.json`, avec le format valide `{ agentId, match: { channel, accountId } }`; ce n’est pas une hiérarchie métier mais un mapping canal/account -> agent.
- Des bindings runtime valides ont été appliqués et le gateway a redémarré avec succès, ce qui a fait passer plusieurs agents de `Routing rules: 0` à `Routing rules: 1`.
- Mission Control a été modifié dans `~/.openclaw/openclaw-mission-control/src/components/agents-view.tsx` pour introduire des groupes métier visuels (INTELLIGENCE, ARCHI, PRODUCTION, OPS, CONTROL, EXPERTISE) reliés à `main`.
- Le build de Mission Control a fini avec succès après correction d’un souci TypeScript sur `GroupNodeComponent`, puis l’instance Next.js sur le port 3333 a été redémarrée.
- Résultat observé : la nouvelle hiérarchie métier est visible dans l’UI, mais l’affichage reste encore trop chargé à cause des edges workspaces/channels/subagents ; une seconde passe UI serait utile pour simplifier et rendre le graphe plus lisible.
