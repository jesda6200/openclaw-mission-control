{
  "name": "crypto-monitor-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node dist/main.js",
    "start:dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "build": "tsc -p tsconfig.json",
    "test": "echo \"No tests yet\" && exit 0",
    "migrate": "node dist/migrate.js || echo 'Run migrations script'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite": "^4.2.0",
    "sqlite3": "^5.1.6",
    "pg": "^8.10.0",
    "redis": "^4.6.7",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "ts-node-dev": "^2.0.0"
  }
}
