# ACP Runtime Configuration

1. Install acpx plugin:
   ```bash
   npm install -g acpx
   ```
2. Enable ACP runtime:
   ```bash
   acpx enable
   ```
3. Verify installation:
   ```bash
   acpx status
   ```

After configuring, restart your session to use ACP capabilities.